# Sanity CMS — create a project at https://www.sanity.io/manage
PUBLIC_SANITY_PROJECT_ID=your-project-id
PUBLIC_SANITY_DATASET=production

# Used for sitemap, Open Graph, and calendar links
PUBLIC_SITE_URL=https://marcobonadies.com

# Optional: webhook secret for deploy rebuilds (set in hosting provider)
SANITY_REVALIDATE_SECRET=your-random-secret
