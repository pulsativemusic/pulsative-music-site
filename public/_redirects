# SPA fallback for Sanity Studio client routes.
# Target /admin/ (not /admin/index.html) so html_handling does not
# strip /index and re-trigger this rule (CF error 100324).
/admin/*    /admin/    200
